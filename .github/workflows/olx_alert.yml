name: OLX Telegram Alerts

on:
  schedule:
    # rulează din 5 în 5 minute (ora UTC)
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  crawl:
    # nu anulăm un job aflat în derulare (deduplicarea are nevoie să termine)
    concurrency:
      group: olx-alerts         # același grup → un singur job simultan
      cancel-in-progress: false

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # 1️⃣ RESTORE — aducem state.json din cache (dacă există)
      - name: Restore state cache
        id: cache-state
        uses: actions/cache/restore@v4
        with:
          path: state.json
          key: dummy
          restore-keys: |
            olx-state-

      # 🔍 DEBUG: arată dacă s-a restaurat ceva
      - name: Show state BEFORE
        run: |
          if [ -f state.json ]; then
            echo "✅ state.json exists BEFORE. Size:" $(wc -c < state.json)
            echo "Preview:"
            head -c 200 state.json || true
            echo ""
          else
            echo "⚠️ state.json NOT FOUND (fresh run)"
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      # 2️⃣ Spider OLX – SONY
      - name: Run spider (SONY)
        env:
          SEARCH_URL:          ${{ secrets.SEARCH_URL_SONY }}
          TELEGRAM_BOT_TOKEN:  ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:    ${{ secrets.TELEGRAM_CHAT_ID }}
        run: scrapy crawl watch -s LOG_LEVEL=INFO

      # 2️⃣ Spider OLX – Camera foto
      - name: Run spider (Camera Foto)
        env:
          SEARCH_URL:          ${{ secrets.SEARCH_URL_CAMERA_FOTO }}
          TELEGRAM_BOT_TOKEN:  ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:    ${{ secrets.TELEGRAM_CHAT_ID }}
        run: scrapy crawl watch -s LOG_LEVEL=INFO

      # 3️⃣ Spider OLX – Aparat foto
      - name: Run spider (Aparat Foto)
        env:
          SEARCH_URL:          ${{ secrets.SEARCH_URL_APARAT_FOTO }}
          TELEGRAM_BOT_TOKEN:  ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:    ${{ secrets.TELEGRAM_CHAT_ID }}
        run: scrapy crawl watch -s LOG_LEVEL=INFO

      # 🔍 DEBUG: inspectăm fișierul după rulari
      - name: Inspect state.json
        run: |
          if [ -f state.json ]; then
            echo "✅ state.json exists AFTER. Size:" $(wc -c < state.json)
            echo "Preview (primele 500 caractere):"
            head -c 500 state.json || true
            echo ""
          else
            echo "⚠️ state.json missing AFTER runs!"
          fi

      # 4️⃣ SAVE — încărcăm state.json actualizat în cache (cheie unică pe run_id)
      - name: Save state cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: state.json
          key: olx-state-${{ github.run_id }}
